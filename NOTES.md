# NOTES — 给后续开发者的交接文档

> README 讲**怎么用**，这份讲**为什么这么做**、**踩过哪些坑**、**接下来做什么**。
> 动这个项目之前先读完这份。很多看起来可以随手改的地方，背后有测过的原因。

最后更新：2026-08-15

---

## 一、这个项目是什么

一个**语音社交推理游戏**：大厅里几张桌子，走过去坐下，一桌人开语音玩阿瓦隆，
人不够就加 AI 补位。视觉参考是 Steam 上的《撒谎酒馆》（Liar's Bar）。

**差异化在 AI 补位。** 撒谎酒馆做了快两年都没做，社区一直在要 —— 不是没人想要，
是难做。这个空白就是这个项目存在的理由。

---

## 二、当前状态（一句话）

> **2026-08-15 转向德州扑克。** 阿瓦隆的规则引擎、LLM agent、防泄漏层
> 全部保留在 `src/game`，**但已经不接界面了**。想回去接上只是换掉 `App.tsx`
> 里的一个组件 —— 那部分文档在下面照旧有效，读的时候知道它当前是停用的即可。
>
> 换的理由和取舍见「九、为什么从阿瓦隆转到德州扑克」。

**两块已经通过事件流接上了，但事件源还是本地假的。**

| 块 | 位置 | 状态 |
|---|---|---|
| 3D 酒馆 | `src/{scene,player,state,ui,audio}` | 两层可走、能选座落座、70 FPS |
| 游戏引擎 | `src/game` | 完整阿瓦隆规则 + LLM agent，纯命令行 |
| 动画编排 | `src/anim` | cue 队列 + 事件桥，会自动加速和瞬时结算 |
| 角色管线 | `src/scene/character` | 动作词汇表 + glTF/程序化双实现 |

两块之间**唯一的接缝是 `GameEvent[]`**。`src/anim/gameCues.ts` 是全项目唯一
知道"某个事件长什么样"的地方；规则引擎不知道动画，动画不知道规则。
事件将来从 WebSocket 来还是从本地引擎来，渲染侧一行都不用改。

### 已经验掉的风险

1. **视觉能不能做到不劝退** → 能。全 primitive、零美术资产，靠光照和后处理撑住
2. **性能** → 9.7 → 70.8 FPS（见下方性能一节）
3. **规则实现对不对** → 规则 bot 基线好人胜率 51.4%（5人）/ 48.7%（7人），落在公开统计区间
4. **LLM 管线通不通** → 通。假 LLM 跑 300 局，64 次非法动作全被吸收，零崩溃
5. **真模型会不会玩** → 已用本地 `claude` CLI 实跑（`npm run game -- --cli --trace`），
   不需要任何 API key。见「七、本地 claude CLI 实测记录」
6. **动画顶不顶得住积压** → 顶得住。事件每 1.6s 来一条、动画一段要 4-6s，
   队列自动从 1.00x 加速到 2.68x 吸收，既不丢事件也不打断正在播的动作

### 还没验的，按危险排序

1. **服务端 / WebSocket** —— 零。前端 store 现在吃写死的 mock，
   事件源是 `src/anim/useDemoGame.ts`（本地跑一局再定时喂出来）。接了 WS 就把它删掉
2. **真语音** —— 只有接收端（`webrtcDriver.ts` 的 AnalyserNode → RMS），**没有任何 RTCPeerConnection**、没有信令、没有 SFU、没有 TURN
3. **美术资源** —— 一个模型都没有。管线做好了（`src/scene/character/models.ts` 加一行即可），
   但 `.glb` 得自己找或自己做。清单为空时全体退回程序化角色，这是设计好的常态
4. **AI 的棋力** —— 管线通了，但没有大样本对局统计。
   本地 CLI 每次调用约 50s，跑不出统计量；要评估棋力得换 API

---

## 三、不要重开的决定

这些是测过或想清楚的，除非有新证据，别推翻。

### 渲染器：three.js + React Three Fiber

不是 Babylon，不是 Canvas 2D。**理由是 React 集成**：这个应用主体是 React
（大厅、房间列表、玩家卡、投票面板），3D 只是其中一个视图。R3F 让 3D 场景和 UI
共享同一个 store。Babylon 的引擎特性（物理、碰撞、导航网格、WebXR）这里一个都用不上。

### 隐藏信息靠类型钉死

`GameState`（含 `roles`）只存在于服务端，对外唯一出口是
`project(state, playerId) → PlayerView`。

**最常见的致命错误是把完整 state 广播出去、让前端负责"不显示"** ——
那等于把梅林是谁写在网络帧里，F12 就赢了。这条规则靠类型强制，不靠自觉。

### 可见性是一张表，不是一串 if

莫德雷德对梅林隐身、奥伯伦对同伙也隐身、派西维尔看到两个人但分不清谁是谁 ——
例外套例外。写在 `rules.ts` 的 `VISIBILITY` 表里，加角色时改表不改逻辑。

### 状态 = fold(事件)

只追加的事件流。重连（重放到当前 + 投影）、回放、AI 上下文、纠纷复盘
全部复用同一条路径，不需要额外代码。

### 相机只有一个所有者

走动、坐下转场、落座环视三种行为全在 `PlayerRig` 里。拆开就会有两个组件
在同一帧写 `camera.position`，那类 bug 极难查。

### 操作方式：标准 FPS（锁指针 + 自由鼠标视角）

查证过《撒谎酒馆》就是这套：WASD 相对镜头、鼠标不按住直接转、Shift 跑、
站到桌边按 E 坐下。**选座是按距离自动选最近的空椅子，完全不用瞄。**

坐下的瞬间解锁指针（牌桌要用光标点牌），按 Q 起身时在按键回调里同步
`requestPointerLock()`（浏览器不接受没有用户手势的锁定请求）。

### 动物头套是工程决策，不只是风格

刚性面具 = 不需要面部绑定、不需要 blendshape、不需要口型同步，还绕开恐怖谷。
所有表达压到肢体语言 + 面具随音量自发光。省下的美术工作量是巨大的。

### 座位号：对内 0 起始，对外一律 1 起始

引擎、投影、事件流里是 `PlayerId`，从 0 开始。
**给人和给模型看的文字里一律从 1 开始。** 转换只在两处做：
`prompt.ts` 的 `n()`（出去）和 `llmAgent.ts` 的 `fromSpoken()`（回来）。

两套编号并存过一次，后果是模型说"0 号有问题"而界面上根本没有 0 号 ——
玩家会指错人，而且这个错怪不到任何一行逻辑上。

改这条时**同时会打破三个地方**，一起改才对：
- `detectLeak` 从发言里抠出的编号是 1 起始，`knowledge` 里是 0 起始，
  不转换的话整层静默偏一位：该拦的放行、不该拦的拦下
- 假 LLM 靠正则从 schema 描述里抠范围。描述从 `0-N` 改成 `1-N` 而假模型没跟上时，
  200 局的非法动作从 38 涨到 **1268** —— 这是发现它的方式
- `leakGuard.check.ts` 的用例文本

`npm run check:leak` 就是为这条转换留的回归网。

### 对局界面不在 3D 里

3D 大厅是进门那一下的氛围和落座；**对局本身是全屏的文字界面**（`src/ui/match`）。

理由不是 2D 好做，是**媒介要和 AI 实际产出的东西对齐**。
差异化是"缺人用 AI 补位"：在 3D 桌上 AI 是个套头套的胶囊做预设手势，
你得假装它有身体语言；在发言流里它和人一样只是一条消息，读起来分不出来。

顺带解决：视野问题消失（人数不再受第一人称构图限制）、
可回溯的发言记录比线下强、AI 不需要 TTS 就能参与。

**发言逐字显示，永远不改写。** 摘要会把"我*觉得*"和"我*确定*"抹平，
而指控经常就挂在这种措辞上。要摘要就另开一栏，原文必须是消息本身。

3D 保留两处用法：进门的大厅，以及将来刺杀揭晓时切回桌上
（`gameCues.ts` 的 assassinated cue 已经写好）。

### 逻辑状态和呈现状态必须分开

`src/game` 里的状态是**真相**，事件一到就更新。
`src/state/useTableStore.ts` 里的是**呈现状态** —— 画面上此刻显示什么，
由动画队列一步步推进，**允许落后于真相，但不允许和真相冲突**。

任何需要呈现状态来做游戏判定的地方都是设计错了：判定只看逻辑状态。
不分开的话，"服务端事件到了但上一个动画没播完"就无解 ——
要么丢事件（状态错），要么打断动画（观感碎）。

配套的一条纪律：**每条 cue 都必须在 `onEnd` 里把状态落定到最终值**，
不能依赖 `onUpdate` 最后一帧刚好跑到 1。队列加速或瞬时结算时 `onUpdate`
可能被整段跳过，只有 `onEnd` 保证执行。

还有一条：**每个"进入某个戏剧状态"的 cue 必须有对应的退出 cue。**
刺杀光柱就是这么留在屏幕上的 —— `ended` 只清了灯光没清焦点，
最后一幕粘住不走。

### 队列积压靠加速吸收，不靠丢弃

`src/anim/cues.ts` 里队列越长播得越快，堆到 12 条直接瞬时结算。
这不是为了好看，是**断线重连一次性重放几十个事件时唯一正确的行为**：
不能让玩家看两分钟回放才追上进度，也不能直接跳过导致状态对不上。

实测：事件每 1.6s 来一条、动画一段 4-6s，队列在 1.00x → 2.68x 之间自适应。

**队列只能有一个推进者。** 两处都在 tick 会让动画跑双倍速，
而且只在特定页面组合下出现，极难复现。和相机只有一个所有者是同一条纪律。

### 角色靠"动作词汇表"访问，不靠 mesh

`src/scene/character/rig.ts` 定义了上层唯一能说的话：
`play('point' | 'lean' | 'shrug' | 'place' | 'recline')` 和每帧的 `RigState`。
上层永远不许碰 mesh、骨骼、材质。

程序化角色和 glTF 角色实现同一个接口，上层分不出区别。
换模型是**替换一个实现**，不是满项目找 `rotation.x`。
反过来做（先啃模型再补接口）美术一改代码全烂。

程序化角色**不是临时占位**，是 glTF 加载失败时的永久兜底：
一个模型 404 就白屏，比一个模型丑得多。

### 座位保持整圈均分，靠后拉相机 + 动态 fov 解决出画

一度打算排成弧形，**算过之后否掉了**。

偏轴角只取决于座位之间的**角间距**：整圈 n 个人，最边上那个是 `90° − 180°/n`。
弧形把 n 个人压进更小的张角，间距反而变小 —— 6 人排成 180° 弧，
邻座从 47° 恶化到 **73.8°**。而 300° 的弧和整圈是同一个几何（都是 60° 间距），
改了等于没改。

真正的两个杠杆是**相机后拉**和 **fov**。现在按人数给不同后拉
（4/5/6 人 → 1.45/1.65/1.80），fov 由 `seatedFov(seats, pullback, aspect)` 实时算。

**fov 绝不能写死。** 同一个 63° 在 16:9 下水平半视角 47.5°、在 3:2 下只有 42.6°，
写死的话换台笔记本或者把窗口拖窄就把边上的人切了 ——
而这种 bug 在自己那台显示器上永远复现不出来。

还有两条一开始漏掉、都是靠实际驱动才发现的：
- **余量要按肩宽算，不是按座位中心点。** 角色半宽 0.3m、距离约 2.9m，
  自己就占 5.3°。只看中心点的话数字上"在画面内"，看起来是被切掉半个身子的
- **自己那把椅子的靠背要藏掉。** 相机后拉之后跑到椅背后面，
  画面下三分之一是一块深色木板。只影响自己这一台，别人看到的椅子照常在

一桌上限 6 人。7 人要把 fov 推到畸变明显的区间，不值得。
**上限写在 `hallLayout.MAX_SEATS`，不在 `game/rules.ts`** ——
规则引擎支持到 10 人是对的，7 人基线还要靠它跑。

### 音频和画面之间只有一个接缝

`src/audio/amplitudes.ts` 是一个模块级 Map，键是 `tableId:seat`。
fakeDriver 和 webrtcDriver 都往里写，3D 从里面读。接真语音时只改一行。

**每帧变的数据不走 React** —— 角色在 `useFrame` 里直接读那块内存。
走 `useState` 会让整棵树每帧重渲染，多桌多路音频下会炸。

---

## 四、踩过的坑（都是实测出来的，别再踩一遍）

### 性能：全部代价在灯上，反射地板和后处理几乎免费

做完二层后掉到 **9.7 FPS**。逐项关掉定位：

| | FPS |
|---|---|
| 原始 | 9.7 |
| 关掉 36 盏点光源 | **52.5** |
| 只关反射地板 | 10.0（几乎没变） |
| 只关后处理 | 11.2（几乎没变） |

直觉会先怀疑 Bloom 和反射，实测它俩加起来只值 1 FPS。**必须量，不能猜。**

根因是"每加一件家具就顺手挂一盏灯"，堆到 46 盏。修法见 `LightBudget.tsx`：
只点亮最近的 N 盏，**且可见盏数必须恒定**（three 按灯的数量编译着色器，
数量一变就重编译，走动时会一顿一顿）。

有了预算系统后，**场景里挂多少盏灯几乎不影响帧率，只有上限值才要钱**，
所以氛围灯该加就加，挂上 `userData={{ budget: 'point' }}` 即可。
曲线在 14 附近有膝盖：之前基本免费，18 开始塌。

### z-fighting：静止正常、一转视角就闪

楼梯的踏板和踢面做得严丝合缝，造出三组**完全共面**的表面。共面的两个面深度相同，
GPU 靠浮点误差决定谁在前，镜头一动就来回翻。

**修法是让实体互相插进去一两厘米，而不是刚好贴合。**

### three 不认 8 位十六进制颜色，会静默变白

`color="#33221600"` 本意是 `#332216`。three 只认 3 位和 6 位，其他长度
**只发一条控制台警告然后保持默认色（白）**。不报错、不崩、只是悄悄变白。
写死的色值值得定期扫一遍位数。

### 着色器吐 NaN → 整个画面全黑，且关掉后处理救不回来

圆锥顶点处四周的法线在插值中相互抵消，`normalize()` 产出 NaN。
**NaN 在 HDR 管线里会传染** —— Bloom 的 mipmap 降采样把一个像素的 NaN 抹遍全图。

最阴的是 **`NaN * 0` 仍是 `NaN`**，所以"把后处理强度调成 0 试试"这个标准排查手段
在这里完全失效，看起来就像"后处理坏了"。修法是 `safeNormalize()` 加长度兜底。

### 移动方向旋转公式符号写错，A/D 正常掩盖了 W/S 颠倒

正确形式是 `x' = vx*cos + vz*sin`、`z' = -vx*sin + vz*cos`。
写错时**纯左右平移因为 `vz = 0` 恰好正常**，只有前后是反的。
改这类公式时四个方向要一个一个代进去验。

### 手感绕了三版才回到最标准的方案

诉求是"按住鼠标才能转很累"。前后试了：世界方向 WASD + 镜头追移动方向（方向感不准，
问题在过渡期）、坦克式 A/D 转向（准了但丢掉侧移，在满是桌子的厅里挪半米要转身三次）、
WASD 侧移 + 方向键转向（能用但不如 FPS 顺手）。

**真正的问题从一开始就不是"怎么让键盘转向"，而是我不该拿掉自由鼠标视角** ——
而拿掉它是为了保住"光标 hover 选座"这个次要交互。一个次要交互把主要手感的上限锁死了。

**教训：连续几版都在调参数却调不好手感时，先怀疑约束本身是不是自己加的。**

### 投影漏了历史，好人胜率只有 2.2%

好人在阿瓦隆里唯一的硬信息是"谁上过失败的任务"，而最初的 `PlayerView` 没有历史队伍记录，
等于让好人闭着眼睛玩。补上之后好人过三轮的比例从 2.7% 跳到 73%。

**LLM 拿到同样残缺的视图会一样瞎，而且隔着一个语言模型这个原因会难查十倍。**
这就是先做无头版的价值。

### 基线一度对自己过拟合

刺客曾用"谁最常否决己方队伍"找梅林，命中率 95%。但那个破绽只存在于
**我自己写的梅林**身上（毫无隐藏地精确否决）。两边都是自己的代码，数字没有意义。

**基线的职责是当尺子，不是自己赢。** 改成随机猜，并把刺杀单列成一个指标 ——
"梅林藏没藏住"和"刺客推没推出来"是两个独立能力。

### 其他

- **`import.meta.env` 需要 `vite/client` 类型**：tsconfig 里显式写了 `types` 数组会覆盖默认行为
- **TS 5.7+ 收紧了 TypedArray 泛型**：`getByteTimeDomainData` 不接受 SharedArrayBuffer 背书的数组
- **透明命中体不能用 `visible={false}`**：three 的 Raycaster 会跳过不可见对象，R3F 指针事件永远收不到

---

## 五、AI agent 的设计

### 假 LLM 是这里最重要的一个决定

`--fake` 是一个确定性的假 completer，读 schema 猜字段、按种子给合法但随意的答案。
**它不"会玩"，也不需要会玩。**

它的职责是让 prompt 构造、结构化输出解析、非法动作重试、防泄漏审查、发言竞价
这整条链路在没有 API key 时也能端到端跑通。

**分清"管线坏了"和"模型不行"是这类项目里最省时间的一件事** ——
这两个问题一旦纠缠，隔着一个语言模型你根本查不出来。

### 非法动作是常态，不是异常

引擎对 agent 完全不信任：队伍不合法就打回重试（3 次），好人想出失败牌直接改成成功，
刺杀目标越界就兜底。**但不默默纵容** —— 非法次数被统计出来，它本身就是个能力指标。
规则 bot 应恒为 0。

### 发言竞价（Werewolf Arena）

每人报 0-4 的发言意愿（0 旁听 / 4 被点名必须回应），最高者发言，
平手时上一轮被点名的人优先。固定轮流发言在阿瓦隆里很失真 —— 真实讨论是抢话的。

**这套现在跑在文字上，接语音时可以原样复用：竞价就是抢麦。**

### 防泄漏审查

AvalonBench 记录过一个结构性失效：**LLM 坏人经常在讨论里自曝身份**。
那不是能力问题 —— system prompt 里明写着"你是刺客，同伙是 3 号"，
模型的注意力就会往那儿泄，靠把提示词写得更严厉压不住。

所以不劝，直接审查（`llmAgent.ts` 的 `detectLeak`）：把私有信息和发言做对照，
命中就打回。纯本地字符串规则，不额外花一次 API 调用。

**关键是这条线要划准**：撒谎是合法战术，泄漏不是。实测中同一句
「我确定 1 号是坏人，因为我看得到坏人是谁」，坏人说被拦（那是交底），
派西维尔说放行（那是诈唬）。

### agent 拿不到完整状态

每个 agent 只吃自己的 `PlayerView`，prompt 也只由这份视图构造。
多个 AI 跑在同一个进程里时，**上下文串味在结构上就不可能发生**。

---

## 六、接下来做什么

### 立刻该做的：权威服务端 + WebSocket

现在**唯一挡在真实可玩性前面的东西**。状态机、事件溯源、per-player 投影、房间大厅。
引擎已经是纯函数了，套一层 WS 广播即可。**这块做错了以后是重写不是重构。**

接上之后，前端要改的只有一处：把 `src/anim/useDemoGame.ts` 换成
WebSocket 推来的 `GameEvent[]`。`useEventBridge` 一行都不用动。

### 之后的顺序

1. **接真语音** —— SFU（mediasoup / LiveKit）+ TURN（coturn）。
   接收端已经写好，缺的是 RTCPeerConnection、信令、SFU。
   **TURN 带宽是真金白银的成本**，别忘了算。
2. **AI 棋力评估** —— 需要 API（本地 CLI 太慢，跑不出统计量）。
   拿结果跟基线比：**好人 51.4%、好人过三轮 73.4%、刺客命中 30%（随机基准 33.3%）**。
   玩得好，后面全是接线；玩不好，需要重估「AI 补位」这个差异化本身。
3. **美术资源** —— 管线已就绪，缺 `.glb`。
   免费方向：Quaternius / Kenney（低多边形，风格接近现在的几何体）、
   Mixamo（骨骼动画库，可以只取动作）。注意各自的授权和署名要求。
4. **静态几何体的光照烤成贴图**（Blender），现在全是实时的。

### 悬而未决、需要拍板的

- 二层挑台宽 5 米，摆下桌椅后绕桌走会蹭栏杆
- 落座后看不到自己的手和牌
- 移动端没测。WebGL + 多路 WebRTC 音频解码在中端手机上会烫，需要低画质档
- 静态几何体的光照应该在 Blender 里烤成贴图，现在全是实时的

---

## 七、本地 claude CLI 实测记录（2026-08-15）

**不需要任何 API key 就能用真模型跑通全链路。** 用本机已登录的 `claude` CLI：

```bash
npm run game -- --cli --trace --players 5 --seed 3 --discussion 0
```

`src/game/claudeCli.ts` 把 `claude -p <prompt>` 当成一个 Completer。
CLI 不支持结构化输出，所以 schema 塞进 prompt，再用**括号配平**的方式
从输出里抠 JSON（`parseLoose`）—— 模型经常把 JSON 包在代码块围栏里，
直接 `JSON.parse` 必挂。

### 结果

```
5 人局 · 种子 3 · 32 次调用 · 797.2s
角色：0=忠臣 1=莫甘娜 2=派西维尔 3=梅林 4=刺客

  3 号提名 [3, 2]        表决 5/5 通过    任务 1 成功
  4 号提名 [4, 1, 0]     表决 2/5 否决（赞成的是 1,4）
  0 号提名 [0, 2, 3]     表决 5/5 通过    任务 2 成功
  1 号提名 [4, 2]        表决 3/5 通过    任务 3 失败（1 张失败牌）
  2 号提名 [0, 2, 3]     表决 4/5 通过    任务 4 成功
刺杀 0 号 → 不是梅林
好人胜（three_quests_good）

非法动作 0　调用报错 0
```

### 这局能说明什么

**能说明的**：管线在真模型下端到端可用。32 次调用零非法动作、零报错，
提示词构造、JSON 解析、动作合法性校验、兜底全部经受住了。

**play 本身也不是随机走子**：
- 梅林首轮提名自己 + 派西维尔，最小的已知好人队伍
- 刺客提名 [4,1,0] 把两个坏人塞进三人任务，被否决，
  **而投赞成的恰好只有 1 和 4 两个坏人** —— 投票模式自己泄了底
- 莫甘娜提名 [4,2] 自己不上、把刺客推上去，成功打进一张失败牌
- 派西维尔复用了此前成功过的队伍 [0,2,3] 收官

**不能说明的**：
- **n=1。** 一局说明不了棋力，只说明链路通
- 明显的失误：梅林（3 号）对 [4,2] 投了赞成票，而他知道 4 是刺客
- 本地 CLI 约 **25s/次调用**，一局 13 分钟。跑不出统计量 ——
  要评估棋力必须换 API（`--llm`）。CLI 的价值是**免 key 验证**，不是跑批

### 顺带修掉的

真模型一局要十几分钟，而事件是跑完才统一打印的 —— 中途零输出会让人以为卡死。
现在每次调用往 **stderr** 打一行进度（走 stderr 是为了 `> log` 重定向时
不污染那份可提交的记录）。

---

## 九、为什么从阿瓦隆转到德州扑克

### 换的理由

扑克天然是**围桌 + 语音 + 筹码**的游戏，3D 大厅那套发牌、飞牌、揭晓的编排
终于有了正经用途，不再只是氛围。阿瓦隆的规则对新玩家太重，
一局二十分钟，原型阶段根本收不到反馈。

一手牌一到三分钟，可重复、可累积，这对留存和对调参都好太多。

### 换来的代价：容错变成零

阿瓦隆算错一个投影没人发现；**德扑算错一次边池，玩家立刻就知道，
而且信任是一次性崩掉的**。所以顺序是先把两个"算错就完蛋"的地方做成可验证的，
再做界面：

1. **牌力评估**（`evaluate.ts`）—— 穷举 C(7,5)=21 种组合打分取最大。
   没用查表法：查表快几十倍，但一张十几万项的表**没法用眼睛验证**。
   21 次组合每手不到 10 微秒，真实瓶颈根本不在这。
2. **边池**（`pots.ts`）—— 按投入额分层。最容易写错的一条是
   **弃牌的人钱要留在池里但没资格争**，直接把他从分层里剔掉会让底池凭空少一块。

`npm run check` 跑全部：`check:poker`（牌力）+ `check:engine`（下注与边池）
+ `check:leak`（阿瓦隆防泄漏，停用但保留）。

### 压力测试比用例更管用

单条用例只能覆盖你想到的情况。真正兜住 bug 的是两个不变量：

- **筹码守恒**：一手牌前后所有人筹码总和必须相等。分错钱几乎必然破坏它。
  4000 手随机牌局 + 400 手 bot 连打，全程守恒
- **边池样本量**：随机对局里 all-in 出现得不够密，所以刻意用参差不齐的起始筹码
  逼出边池。现在 4000 手里有 2698 手产生边池 —— 用例数字掉下去就说明覆盖没了

`settle()` 里那条守恒断言是**故意抛错而不是记日志**的：分池写错时它立刻炸，
而不是等玩家发现自己少了钱。

### 已经踩过的坑

- **单挑规则和多人局相反**：钮位是小盲，且翻牌前钮位先说话。写反了不会报错，
  只会让所有单挑局的位置全错
- **不足额的 all-in 不重开下注权**。漏掉这条，短码 all-in 会变成无限重开的工具
- **摊牌标签必须带踢脚牌**。两家都是"两对 Q 和 8"、一个赢一个输时，
  标签一样等于告诉玩家"引擎乱判"。摊牌界面的职责不只是报结果，是让人看出**为什么**
- 三次测试用例写错、代码是对的（边池合并、同花顺用例根本没有同花）。
  **用例本身也会错，`✓` 不等于验证过** —— 每条用例都该问一句"它失败时会怎样"

### 合规：只能是虚拟筹码

澳洲 *Interactive Gambling Act 2001* 对真钱在线扑克管得很死。
**不能买筹码、不能提现**，界面上那行"虚拟筹码 · 不可充值提现"是产品定义，
不是免责声明。这条一旦碰，项目性质完全改变。

---

## 十一、牌桌界面的第二版（2026-08-15）

第一版被否了，原话是"太丑了，看着恶心"。**根因不是配色，是断裂**：
玩家刚从一个昏暗暖光、有氛围的 3D 酒馆走出来，落地是一块扁平绿椭圆加系统字体。

所以这一版的第一原则是**接回大厅**：暖琥珀 + 暗木 + 吊灯光锥 + 重暗角，
并且加了一层胶片颗粒 —— 3D 那边后处理里有（强度 0.06），
两边"材质"对不上的话，切换时会觉得进了另一个软件。

### 筹码必须是实体

"底池 340" 要读、要换算；一摞筹码一眼就知道多。
扑克桌的信息密度本来就靠筹码堆传达，换成数字等于把游戏抽象成了表格。
面额配色刻意避开赌场那套高饱和红蓝绿 —— 这是家昏暗的酒馆。

### 牌的信息在角标上

第一版把点数放牌面正中，看起来像字母卡片。真牌的信息在**左上角**，
因为手里捏成扇形时只看得见角。差别听起来很小，但它就是"像不像真东西"的全部。

### 按钮只说了"能做什么"，没说"现在是什么情况"

新手盯着「弃牌 / 过牌 / 加注」没有任何依据去选。所以加了 `coach.ts`：
当前是第几条街、你手里成了什么牌、谁下了多少、跟这一注需要多少胜率才划算。
每个按钮再带一句解释。老手会自动忽略，新手不会卡住 —— 这个不对称是它值得占屏幕的理由。

**教学面上误导比不说严重得多。** 第一版把 3♦2♥ 描述成"32 连张，有做顺子的机会"——
那是德扑里最烂的牌之一，而这句话会直接让新手去跟注。
现在强弱和牌型一起说，而且**强弱直接复用 bot 用的 `handStrength`**：
两套评估迟早会给出互相矛盾的建议。

### 摊牌要高亮是哪五张赢的

只报"两对 Q 和 8"，新手对不上是哪几张牌。
`showdown` 事件现在带 `best`，界面据此高亮 —— 这是整局里最强的一次「啊原来如此」。

### 顶栏两个说明浮层

「牌型大小」直接把牌摆出来，不是写一行字；「怎么玩」讲一手牌的流程。
"同花和顺子谁大"不该逼人去开浏览器搜。

---

## 十二、UTS 元素（2026-08-15）

酒馆里加了两处向 UTS 致敬的东西，都是程序化生成，**零外部资源、零光源预算**
（靠 emissive + 已有的 Bloom）。

### 「刨丝器」幕墙

Building 11（工程与 IT 学院楼）的铝板穿孔图案不是装饰：它是
"University of Technology Sydney Faculty of Engineering and Information Technology"
这句话的**二进制编码**，夜里由绿色 LED 从背后打亮。

`BinaryFacade.tsx` 里的每一个亮格都是真的一位 —— 同一句话按 8 位 ASCII 展开。

**踩了两个坑，都值得记：**

1. **mipmap 把细节平均没了。** 第一版用 72×26 的网格、每格画一个小圆点，
   结果贴到脸上发光、退开三米整块墙是黑的 —— 远处贴图逐级缩小，
   亮点和深色底被平均成了黑。
   修法不是调亮度，是**把亮格填满**：平均之后仍然是绿的。
   网格也放疏到 48×16。

2. **z-fighting，而且伪装成了材质问题。** 边框盒子写成 `position z = -0.03`、
   厚 `0.06`，前表面正好落在 `z = 0`，和面板**共面**。
   远处深度精度不够时边框赢，整块墙是黑的；贴到脸上精度够了才轮到面板。
   表现是"近看有、退开就没了"，非常像贴图或 emissive 出了问题。
   NOTES 楼梯那节记过同一条坑，这次又踩了一遍。

   排查时还绕了个弯：单点读像素一直是黑的，因为采样点正好落在正中那根
   「鳃」上。**改成采整个面板区域取统计值才看出真相**（最大绿 255、30% 像素发绿）。

### 建筑模型陈列

`CampusModels.tsx`：UTS Tower（粗野主义，1979）、Dr Chau Chak Wing
（Gehry 的"皱纸袋"，2015）、UTS Central（扭转塔，2019）。

"皱纸袋"的起伏用**两个不同频率的正弦叠加**——单一正弦会得到规整波浪，
看起来像瓦楞板而不是被捏皱的纸。

牌桌上补位 AI 的名字也换成了 Ultimo / Broadway / Haymarket / Dysart / Gehry / Alumni。

### 发光的 UTS，铺在动线上

`GlowText.tsx` + `UtsDressing.tsx`：进门脚下的地面、中庭天花板、吧台上方的斜体霓虹、
三面挑台边缘的灯带、楼梯每隔四级的踢面、二楼挑台地面。

**位置是按走位挑的，不是按墙面挑的** —— 进门踩到一个、抬头看见一个、
走向吧台看见招牌、站在中庭被三面灯带围住、上楼一级级点过去。
分散在动线上才是"充满"，贴在几面墙上只是海报。

实现是背景透明的 canvas + 加色混合，所以只有笔画发光、底完全看不见，
贴在任何颜色的表面上都不会露出方块。同样不占光源预算。

踩到的一个小坑：**canvas 的留白必须大于最大模糊半径**。
留窄了，光晕在贴图边缘被硬切，画面上就是发光字周围浮着一个方框 ——
而且它只在暗背景上看得出来。

### 挑台和楼梯改用徽记

一排 `UTS · UTS · UTS` 的字带太密，远看像贴纸。换成 `GlowEmblem.tsx` ——
同一套发光处理，形状来自 `emblem.png`。

**这是项目里第一个外部图片资源**，之前全是程序化生成的。
Vite 会把它带上正确的 base 路径打包，不用手动处理。

处理方式是**盾牌发光、内部图案镂空**，像背光灯箱。
反过来（只让白色图案发光）会得到一堆互不相连的碎片，远看认不出。
透明度用 `alpha = 原alpha × (1 - 亮度)`：不管源图背景是白色还是已经透明，
结果都一样，少一个会静默出错的前提。

楼梯那组绕了个弯：先贴在踢面上，结果被踏面挡得几乎看不见 ——
楼梯是一级级离散台阶，而 `stairHeightAt` 是连续斜坡函数，两者对不齐。
最后改成**平躺在踏面上**，而且上楼时视线本来朝下，反而更显眼。

### 第三版：做减法

铺满之后反而看不清，所以往回收：

- **天花板那个去掉了。** 抬头才看得见，存在感和它占的面积不匹配
- **挑台从等距一整圈收成一边一个。** 十几个等距排开，眼睛没有落点，
  只剩"这里有一堆绿色的东西"；一边一个反而像正经标识
- **幕墙改成点阵招牌**（`SignWall.tsx`）。先是把二进制铺满整墙、
  再把校徽和字压在上面 —— 两层内容叠在一起，谁也看不清。
  第四版删掉了叠加：**点亮的格子直接拼出 UTS**，其余格子熄灭当肌理，
  校徽单独放在左边。整面墙只剩一层内容。

  **画的顺序错了会很难看，这一块返工了三次。**

  第一次：格子太大（52×18），而且每格四周留白、中间点一个白芯。
  凑近看就是一串互不相连的小方块 —— 那是"很多颗 LED"，不是"一块背光的板"。
  这个分辨率下 S 尤其惨，曲线必然断成几截。

  最后的做法是照着**真实背光穿孔板的物理顺序**画：
  光源在背后是连续的，网格是盖在光上面的缝。所以先铺连续的亮区（亮格填满整格、
  不留内边距，相邻格自然连成一笔），最后整块板统一压一层缝，亮的暗的一视同仁。

  格子同时细到 104×36（单格约 7 厘米，和真孔距一个量级）。
  行数够了之后，字形就可以放心交给字体渲染了 —— 18 行时非手排点阵不可的问题，
  提高分辨率之后自己就没了。

  熄灭的格子也要画出来，没有它们就不是一块穿孔板，只是几个飘着的方块。

  校徽和字对齐时踩了一个坑：`GlowEmblem` 的 `size` 是**平面边长**，
  平面里还有一圈留白（给光晕用），图案只占中间 `EMBLEM_ART_FRACTION`。
  直接把 size 设成字高，画出来会明显偏小，而且小得很难解释
  （"我明明设成一样大了"）。现在这个比例是导出的常量，谁要对齐谁去除。
- **楼梯徽记从 0.5 米缩到 0.26 米。** 踏面进深只有 0.4 米，
  比踏面还大的东西会被下一级挡掉一半

一条通用的：**"充满"靠的是分散在动线上，不是靠密度。**

### 一句提醒

这些都是自娱自乐的装饰。**如果哪天要商用或者当作品集对外推广，
先把这一层清掉或者去问一下 UTS** —— 现在的用法是按"个人玩"来的。

---

## 十、写出来的部分

[docs/how-i-know-it-works.md](docs/how-i-know-it-works.md) ——
把这个项目里的验证手段整理成了一篇文章：不变量优于用例、可验证优于快、
假 LLM 必须和真的说同一种协议、两套编号约定并存时没有任何一行代码是错的、
以及那次「关掉后处理还是全黑」为什么排除错了（`NaN × 0` 仍是 `NaN`）。

改这些机制之前建议先读一遍，里面每条都是踩出来的。

---

## 八、参考资料

- **AvalonBench**（arXiv:2310.05036）—— LLM 玩阿瓦隆的 benchmark。
  代码 `github.com/jonathanmli/Avalon-LLM`，只支持 5 人局，无派西维尔/莫甘娜/莫德雷德。
  核心结论：GPT-3.5 当好人胜率 22% **低于**规则 bot 的 38%，且经常自曝身份
- **Werewolf Arena**（arXiv:2407.13943，Google Research）—— 发言竞价机制的出处
- **Verbal Werewolf**（arXiv:2506.00160）—— LLM + TTS 双管线并行，近实时
- **Trust, Lies, and Long Memories**（arXiv:2604.20582）—— 多局阿瓦隆的跨局记忆与声誉
- **ReCon**（ACL 2024 Findings，arXiv:2310.01320）—— 一阶/二阶视角转换对抗欺骗
- 综述索引：`git-disl/awesome-LLM-game-agent-papers`

---

## 十三、从"一个扑克"变成"一张能开三种游戏的桌子"（2026-08-22）

原来的形态是：坐下 → 直接进德州扑克。现在是：坐下 → 选今天打什么 → 对应游戏接管。
**2D 层不是缺陷，是特性** —— 正因为牌桌是盖在 3D 上的一层而不是替换掉它，
换游戏才只是换这一层，酒馆一直在背后。

### 架构

- `src/games/registry.ts` —— 有哪些游戏、各自几个人
- `src/games/seats.ts` —— 这一局的名单。**人数由游戏决定，不是椅子数**：
  六人桌开斗地主就坐三个人，剩下的椅子空着
- `src/ui/SeatedOverlay.tsx` —— 没选就出面板，选了就把台面交出去。
  每个游戏组件只在被选中时挂载，不挂载就不会有第二局在后台偷偷跑

### 斗地主

`src/ddz/`。牌型识别（`combo.ts`）是这个游戏唯一真正难的地方，
所以入口只有一个 `parse(cards)`：界面收到玩家勾了哪几张，扔进来，
认得出就是合法出牌。**不存在"先选牌型再选牌"那种交互** ——
那种做法迟早会让界面和引擎对牌型的理解分家。

#### 覆盖率加密之后立刻抓到一个真 bug

第一版的随机检查是"随机切几张牌当上家"，结果 1500 轮里只有 310 轮
真的构造出了合法牌型 —— 「必须压住」那条路径基本没跑到。
改成从另一副手牌里枚举一个合法打法当上家，覆盖率上去之后，
第一秒就抓到 `beats(王炸, 王炸)` 返回 `true`。

一副牌里只有一个王炸，**实战永远撞不上这一格**，写成 `return true`
不会有任何人发现。但那样这个关系就是自反的，
而"谁压得住谁"必须是严格偏序。

顺带加了一条断言：十四种牌型必须**每一种都在随机流里出现过**。
少一种说明枚举漏了一整类，而这种漏法不会让任何用例变红。

#### bot 不会打炸弹，是量出来的，不是看出来的

覆盖率断言先报警：四千局里炸弹只被打出 140 次，而王炸打了 879 次。
我第一反应是"门槛定高了"，改了两版，**越改越少（140 → 118）**。

停下来直接量：炸弹和王炸的到手率是**一样的**（都是 32.6%）。
再追这四张牌最后去哪了 ——

```
作为炸弹打出：11.8%
被拆开打散：  84.7%
攥到结束没动：3.5%
```

原因在成本模型里：bot 全部的判断力是"出完之后还要几手"，
而按这个标准**拆炸弹是免费的** —— `3333` 拆一张去凑 `34567` 顺子，
手数从 5 掉到 2，模型看着是天大的好事。

炸弹的价值根本不在手数里：它压得住一切，还翻一倍。
加上"拆掉一个炸弹单独记一笔重帐"之后，作为炸弹打出的比例 11.8% → 65.8%，
和真人打法对上了。

**教训还是那个：先量，再改。** 我那两版凭直觉的修改不但没用，
还把方向带偏了。

### 卡坦岛

`src/catan/`。三个游戏里规则面最宽的一个，所以写法也不一样：
不写"一手牌的流程"，而是**状态机 + 合法动作枚举**。
`legal(state, seat)` 列出此刻能做什么，`apply()` 执行一个。

好处是**界面和 bot 吃同一份合法性**：按钮灰不灰、棋盘上哪些路口亮起来，
都来自同一个 `legal`。分成两套的话，界面允许的和引擎允许的迟早会分家。

#### 几何和图结构是同一份数据

把每块地的六个角按坐标算出来、去重，得到的既是画 SVG 要的点，
也是"哪个路口挨着哪个路口"的邻接表。分成两套的话，
**画出来挨着但规则说不挨着**，这种 bug 找起来要命。

去重踩了一个坑：坐标当 key 要先量化成整数，**不能用 `toFixed`**。
`(-1e-16).toFixed(3)` 是 `"-0.000"`，而 `(0).toFixed(3)` 是 `"0.000"` ——
同一个点算出来的两个坐标只差一个符号位，就变成了两个不同的路口。
症状是 54 个路口变成 56 个、72 条路变成 76 条，还冒出四个度数为 4 的路口，
而六边形网格里根本不存在这种点。

#### 资源守恒抓到的那个 bug

盖东西花掉的资源**没有还给银行**。卡坦的资源是一副有限的牌，不是计数器。
一局下来 95 张只剩 31 张，银行发空，产出停摆，谁也到不了 10 分。

场面上看不出任何异常，只是"大家都很穷"、每局要打两万步还分不出胜负。

#### 检查脚本报了一句它从来没验过的话

这个更值得记。跑不完的局在守恒检查之前就 `continue` 掉了，
而脚本末尾照样打印「资源守恒、发展卡守恒、组件守恒，全部成立」。
**300 局全部没跑完的那次，这句话依然出现在屏幕上。**

比测试挂掉危险得多。现在加了一个 `audited` 计数：
真正跑到检查的局数少于总局数，本身就是一条失败。

#### bot 囤牌是个打法问题，不是性能问题

第一版 bot 只在"换完立刻能盖"的时候才换银行、铺路还要看心情，
于是一路囤到十几张。三百局里弃了 11551 次（平均每局 38 次），
一局要打 131 个回合。

**手上超过七张就会被强盗抓着弃一半，所以攥牌是有成本的** ——
把这一条写进 bot 之后：弃牌 38 → 12.7 次/局，回合数 131 → 102。

### 顺带修掉的

坐到四座桌子上，大厅的环境动画会抛「不支持 4 人局」——
那个假事件源跑的是阿瓦隆，而阿瓦隆最少 5 人。这个 bug 一直都在，
只是以前很少坐四人桌。

### 还没做的

- **卡坦没有玩家之间的交易**，只有和银行/港口换。
  和 bot 讨价还价要做得不像摆设，需要的东西比规则本身还多
- **港口位置不是官方那套**。官方印死在板子上，而这里每局盘面都不一样，
  所以是沿海岸等距取的。数量、种类、汇率配比按官方
- 三个游戏的 LLM agent 都没接。`src/game/llmAgent.ts` 那套还只服务阿瓦隆

---

## 十四、大厅改成 2D，3D 降级成背景（2026-08-22）

原来的入口是"走进 3D 酒馆 → 走到桌边 → 按 E 坐下"。现在是
"2D 房间列表 → 挑一行进去 → 等待室 → 开局"。

### 为什么

**棋牌室的大厅是拿来扫的，不是拿来逛的。** 玩家在找的是
"哪间正在等人、打的是我要的游戏"，走过去要十几秒，而一行文字零点几秒就答完。
第一人称走位是氛围，但它挡在入口上，每一局都要付一次这个代价。

3D 没有删。它降级成背景，继续渲染，里面那桌人一直在打
（`useDemoGame(true)` 常开，`BackdropCamera` 自己慢慢漂移，不吃任何输入）。
**那座酒馆是这个东西和一个普通网页棋牌室之间唯一的区别**，删掉就只剩一个列表。

### 一行房间里信息的排布

按"扫"来定，不是按重要性：

1. **左端色条** —— 什么游戏。这是余光里最先到达的信息
2. **状态灯** —— 进得去吗
3. **人数点阵** —— 满没满是一眼的事，不该让人做减法，所以是格子不是数字
4. **房间名** —— 排在最后。它其实是最不重要的一项

### 画风

亮、卡通、粗描边，和牌桌那边刻意不同。**进房间之前和之后是两种明度** ——
跑跑卡丁车和弹弹堂都是这个分工。

卡通感靠三件事，不靠贴图：粗描边（所有东西 2.5–3px 深棕）、
**实心下沿**（`box-shadow: 0 4px 0`，按下去整块往下沉，比模糊阴影更"有厚度"）、
饱和但暖（橙黄为主，和背后那座酒馆同一族颜色）。

### 两个自找的坑

**一、`font: 800 14px/1 inherit` 是非法 CSS。**

`font` 简写的字体族那一格不接受 `inherit`，整条声明会被**整个丢掉**。
我这么写了 30 处，结果大厅里所有字重字号全部没生效 ——
标题细得像正文，按钮像纯文本。而浏览器不报任何错，
DevTools 里那条声明就是不在。

改成 `var(--font)` 就好了：var() 是在解析之前替换的。

**二、CSS 变量挂错了地方。**

那套皮肤要给三个**互不嵌套**的根元素用：大厅、进场页、弹窗。
我只把变量定义在 `.lb` 上，于是进场页拿不到 `--ink` / `--go`，
描边和下沿全没了，看起来像没做完。

两个坑都是同一类：**改完之后长得不对，但什么都没报错**。
这个项目里最耗时间的 bug 一直是这一类。

### 进别人的房是条死路

按钮写着"等房主开始"，而那个房主是个 bot，永远不会按。
这不是拟真度问题，是**走不通**。改成 AI 房主自己倒数三秒开局 ——
顺带也更像真的大厅：你进去，坐一会儿，局就开了。

### 顺带清掉的

`Hud.tsx` 和 `GamePicker.tsx` 删了，连同它们那几百行 CSS。
走动 HUD、准心、进场遮罩、同桌玩家列表都是第一人称时代的东西；
选游戏挪到了建房的时候，因为**大厅那一行必须提前说清楚这间打什么**，
点进去才发现不对是棋牌室大厅最讨人厌的一种体验。

### 还没做的

- 房间是本地 mock，没有服务端。接了 WebSocket 之后要换的只有
  `useLobby` 里的 `rooms` 一处，界面一行不用改
- 房间里没有聊天，也没有准备/踢人
- 3D 那边 `PlayerRig` / `SeatPicker` 还留着但不再挂载。
  什么时候确定不做"走进去"这条路了，再一起清

---

## 十五、把三个游戏和大厅做到能顺手玩（2026-08-23）

这一轮不加新游戏，只做三件事：**卡坦补完最后一条规则、大厅补上房间机制、
全局补上音效和动效**。目标是"能丝滑地玩起来"，不是"功能齐了"。

### 卡坦的玩家间交易

引擎保持纯的，只提供 `canTrade` 和 `applyTrade`。
**谁愿意接是对局流程的事，不是规则的事** —— 放进 `apply` 里就等于
让引擎去 await 别的 agent，那条线一旦拉出来就收不回去了。
所以问一圈放在 runner，按座位顺序问，先答应的先成交。

bot 应答有三条判据，第一条最重要：**领先的人别帮**
（`publicVp >= 8` 直接拒）。少了它，bot 会一路把冠军喂到底，
那比不会换更糟。

#### 提议频率是量出来的

第一版 45%，三百局提了 26415 次 —— 摊到每局 88 次，
也就是**每个回合都要被问一次**。bot 之间无所谓，
但只要桌上有个真人，那就是每回合弹一次窗，比不能换还烦。
压到 16% 之后每局 22 次，成交率 21%。

人这边的应答面板**必须能超时**。玩家去倒杯水，整局就卡在这里等他 ——
单机只是烦，联机是把三个人一起冻住。超时当拒绝。

### 音效：现场合成，没有音频文件

和整个项目零美术资源是同一条理由。一套音效是资源、加载和授权三重麻烦，
而 UI 音效本来就是几十毫秒的包络问题：一段噪声加一条衰减曲线。

只守两条：**短**（超过 120ms，连点时会糊成一片）和**轻**。

按钮声用一个委托监听器，不给每个按钮挂 onClick ——
这个项目里上百个按钮，分散着挂迟早会漏，
而漏掉的那几个会让人觉得"这里点了没反应"，比全都没声音更糟。

静音开关必须在牌桌里也有。只在大厅放一个的话，
打到一半想关就得先离席，那等于没有。

### 动效：每一条都对应一个认知问题

不是装饰。换屏了 / 这个东西是新出现的 / 你刚才点的是它 / 这里不能点。
房间行逐条错开进场，意义不在好看，而在**让人看出这是一份刚刷出来的列表** ——
整块一起淡入的话，翻页和刷新看起来跟没反应一样。

系统级的「减少动态效果」一律尊重。这不是照顾少数人，
前庭功能敏感的人看这类位移会真的难受。

### 三个走不通的地方

都是做完之后自己走一遍才发现的，用例一条都测不到。

**一、进别人的房是条死路。** 按钮写着"等房主开始"，而那个房主是 bot，
永远不会按。改成 AI 房主自己倒数三秒开局。

**二、打完一局回房间，又被自动开局弹回去。** 于是回不去大厅 ——
唯一的出路是在牌桌里点"离开"，而那时玩家已经以为自己按错了。
加了个 `finished` 标记：**打完之后不再自动开**，
而且"再来一局"对所有人可点，不用等房主。

**三、德州输光筹码之后，牌桌就那么停在那里**，只有日志里一行小字。
**没有出口的静止画面看起来就是卡死。** 补了结束面板。

### 窄屏

房间行原来列了四列，而实际有六个孩子，多出来的两个折到下一行，
每行都戳出半个按钮。现在按"扫"的优先级砍：
砍掉游戏名（左端色条已经说了）、砍掉人数点阵（留数字）、
砍掉状态标签（进不去的行本来就是灰的）。**列数必须和真正显示的孩子数对上。**

### 斗地主的出牌位置

原来每个人出的牌摆在他自己那一栏里，中间空着一大片，
而要比较大小的三手牌散在屏幕三个角上。改成三家都往台面中间聚 ——
这是真牌桌的排法，也是唯一能一眼看懂这一轮打成什么样的排法。

---

## 十六、把阿瓦隆整条线删掉（2026-08-24）

一次删了 4186 行，占动手前源码的四分之一。

### 它为什么还活着

`src/game/`（2126 行阿瓦隆规则引擎 + LLM agent）不是躺着的死代码 ——
它**还在跑**：本地开一局规则 bot 的阿瓦隆，把事件翻成动画 cue 喂给 3D 场景，
让背景里那桌人动起来。

顺着查下去才发现那一整条链上真正可见的只剩**一个前倾动作**：

- 提名高亮、投票筹码、飞向中央的任务牌、刺杀聚焦，
  全都写进 `TableChoreography`
- 而那个组件的渲染条件是 `seatedAt?.tableId === table.id`
- 大厅改成 2D 之后**再没有人坐进 3D**，`seatedAt` 恒为 null

所以它一次都没渲染过。为了一个前倾动作养着两千多行规则引擎，不划算。

换成 `tavernLife.ts` —— 四十行，随机挑个座位做个动作，间隔带抖动。
**背景需要的是"有人在动"，不是"在打一局什么"**：
隔着一层暖纱、在几十米外，谁也分不出那桌人在打阿瓦隆还是在聊天。

它仍然走 cue 队列而不是直接调 `gestureAt`。队列保证两个动作不会叠在一起，
而这正是 `cues.ts` 存在的理由，不该因为事件源换了就绕开它。

### 顺手抓到一个假承诺

椅子还在地上画青色光圈（"这个位置可以坐"），还准备着 `E 坐下` 的提示牌 ——
而那把椅子早就坐不进去了。提示牌因为挂在 hover 上没露出来，
光圈却一直亮着，因为它挂的是 `mode === 'walking'`，而那个 mode 再也不变了。

**一个还在发邀请的、点了没反应的东西，比没有它更糟。** 椅子现在只剩木头。

### 一起走的

| | 行数 | 为什么 |
|---|---|---|
| `src/game/` | 2126 | 阿瓦隆规则引擎、prompt、LLM agent、防泄漏审查 |
| `src/ui/match/` | 679 | 阿瓦隆界面。只有 `Avatar` 还在用，提到 `src/ui/` |
| `src/player/` | 613 | 第一人称走位。大厅改 2D 之后再没挂载过 |
| `src/match/` | 248 | 阿瓦隆的对局 store |
| `TableChoreography` | 224 | 投票筹码、任务牌、刺杀聚焦。从未渲染 |
| `gameCues` / `useDemoGame` | 240 | 阿瓦隆事件 → 动画 cue |
| `useTableStore` | ~100 | 呈现状态，字段全是阿瓦隆的 |

`usePlayerStore` 原来装着走动/落座/起身的模式机、坐在哪把椅子、
光标指着哪把、指针锁定状态。现在只剩一个布尔值，改名叫 `useEntered`。
它还留着是因为**那一次点击本身有用**：浏览器不允许没有用户手势就
resume AudioContext，而这个项目现在有音效。

`@anthropic-ai/sdk`、`npm run game`、`npm run check:leak` 一起走。
**现在这个仓库没有任何需要 API key 的东西。**

### 该不该留着"以后可能用回来"

`src/player/` 那 613 行是有质量的代码 —— 相机单一所有者、
走动和落座的转场、按距离自动选座（那是为了不牺牲手感而改掉 hover 选座的结果）。
留着的理由是"万一要做回 3D 视角"。

但它在 git 里。删掉不等于失去，而留着**一定会**让每个打开这个仓库的人
先花几分钟弄明白它是不是活的。这笔账很好算。

### 顺带：背景终于看得见了

大厅改 2D 之后，那座酒馆被压到只剩几条轮廓。它是这东西和一个普通网页
棋牌室之间唯一的区别，压到看不见等于白养着。

三处一起调：

- **环境光 0.16 → 0.26。** 原来那个值是按"玩家走进去、离桌子两米"定的：
  近处有吊灯的主光，环境光只负责把暗部从死黑里拉出来一点。
  现在相机退到中庭当背景，画面里九成是远处，主光根本照不到。
  **这不是"调亮一点"，是场景的用途变了。**
- **纱压得轻一些**（边缘 90% 的黑 → 73%，亮度 0.62 → 0.86）。
  列表本身是不透明的奶油板子，可读性靠它，不靠把背景抹黑。
  进场页压得更轻 —— 那一屏没有任何面板挡着，是唯一一次能好好看那座酒馆的机会。
- **角色头顶的名牌删了。** 名牌是"你和这些人同桌"时才有意义的东西，
  现在没人同桌，它就变成了漏进画面的 UI —— 几个名字飘在背景的酒馆里，
  还带着 AI 徽标。背景该像一幅画，不该像一个开着调试信息的游戏。

### 顺带：你终于有名字了

之前是硬编码的 `{ name: '你', color: '#e2743a' }` —— 每次刷新都是同一个
没有名字的橙色圆点。房间列表、聊天、牌桌上全是"你"，
没有任何一处说明这是同一个人。**那是整个东西里最像 demo 的一处。**

现在昵称和头像色存在 localStorage 里，点右上角自己的名字就能改。
**没有账号系统，也不打算有** —— 这是个本地跑的东西，
昵称属于这台浏览器，不属于任何服务器，面板上把这句话写出来了。

牌桌上自己那一栏也显示昵称，不再写"你"。**那是别人看到的名字，
自己那一栏该是同一个** —— 是谁靠高亮区分，不靠改称呼。

顺手把大厅的筛选也记住了。只想打斗地主的人，每次进来都要先点一下，
一次不烦，第五次就烦了。这种"上次怎么样"的小记忆，
是"顺手"和"能用"之间的差别。

读写 localStorage 全部包 try/catch：无痕模式下它直接抛异常，
而"记不住昵称"不该让整个应用起不来。
